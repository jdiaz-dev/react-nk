import React, { Component } from 'react';
import { Button } from '@material-ui/core';

export class OpenersContainer extends Component {
  render() {
    return (
      <>
        <div>OpenersContainer</div>
        <Button>Create </Button>
      </>
    );
  }
}

export default OpenersContainer;
